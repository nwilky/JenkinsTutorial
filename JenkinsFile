pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello from Jenkins!"'
            }
        }
        stage('deploy') {
            steps {
                sh 'echo "Deploy to Staging Environment!"'
            }
        }
    }
}
